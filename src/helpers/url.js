const DB = "https://stocktaking-db.herokuapp.com";

const url = {
  front: "http://localhost:3000",
  backTools: `${DB}/api/tools`,
  backUsers: `${DB}/api/users`,
  backReports: `${DB}/api/reports`,
};

export default url;
